{
  "name": "@gpt-ui/llm-engine",
  "version": "1.0.0",
  "description": "Pluggable LLM engine with Ollama-first architecture",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@gpt-ui/schema": "file:../schema"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}